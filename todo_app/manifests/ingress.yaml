apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /local-image
            pathType: Prefix
            backend:
              service:
                name: todo-app-svc
                port:
                  number: 2345
          # - path: /api
          #   pathType: Prefix
          #   backend:
          #     service:
          #       name: todo-app-svc
          #       port:
          #         number: 2346
